# แผนพัฒนา: ระบบนับจำนวนคนแบบ Real-time (Moving Camera)

| | |
|---|---|
| **เวอร์ชัน** | 1.0 |
| **วันที่** | 21 สิงหาคม 2026 |
| **สถานะ** | รอ confirm เริ่ม Phase 0 |
| **Target** | PC + Webcam (หลัก) → Mobile on-device (ภายหลัง) |

---

## 1. ภาพรวมโปรเจกต์

### 1.1 เป้าหมาย

นับ **จำนวนคนไม่ซ้ำ (unique person count)** แบบสะสม จากกล้องที่ **เคลื่อนที่ได้** ประมวลผลแบบ real-time โดย **ไม่บันทึกภาพหรือวิดีโอ** ใน production

### 1.2 Scope

| ✅ In Scope (MVP) | ❌ Out of Scope |
|---|---|
| ตรวจจับคนด้วย deep learning | บันทึกวิดีโอ/ภาพนิ่ง |
| Track คนข้ามเฟรม | แยกเพศ / อายุ / ใบหน้า |
| นับสะสมไม่ซ้ำคน | Multi-camera / cloud dashboard |
| แสดงผลสดบนหน้าจอ | Face recognition / ระบุตัวบุคคล |
| Log ตัวเลข (ไม่มีภาพ) | Alert / notification |
| สลับ camera source ได้จาก config | Web UI / REST API |

### 1.3 Success Metrics

| ตัวชี้วัด | เป้าหมาย | วัดอย่างไร |
|---|---|---|
| Count Error | ≤ 15% | เทียบกับ ground truth ที่นับด้วยตา |
| Throughput | ≥ 20 FPS | สูงกว่ามาตรฐานเพราะกล้องถือเดิน — ดูหัวข้อ 1.5 |
| ID Switch | ≤ 2 ครั้ง / คลิป 1 นาที | นับจาก log |
| Startup Time | ≤ 10 วินาที | จับเวลา |

### 1.4 ข้อจำกัดที่ยอมรับตั้งแต่ต้น

> ⚠️ **กล้องเคลื่อนที่ + นับไม่ซ้ำ คือโจทย์ที่ยากกว่ากล้องนิ่งมาก** — background subtraction ใช้ไม่ได้เลย ต้อง detect เต็มทุกเฟรม และ motion prediction ของ tracker จะเดาพลาดบ่อยเพราะทั้งกล้องและคนขยับพร้อมกัน
>
> **ไม่คาดหวังความแม่นยำ 100%** โดยเฉพาะเคส: คนเดินออกนอกเฟรมแล้วกลับเข้ามาใหม่, คนเดินสวนกันจนบังกัน, ฝูงชนหนาแน่น

### 1.5 ผลกระทบเฉพาะจาก "กล้องถือเดิน" 🔴

การถือกล้องเดินคือรูปแบบที่ยากที่สุดในบรรดากล้องเคลื่อนที่ทั้งหมด (ยากกว่ากล้องหมุนบนขาตั้งมาก) เพราะ:

| ปัญหา | สาเหตุ | ผลต่อการนับ |
|---|---|---|
| **ภาพสั่น + motion blur** | การก้าวเดินทำให้กล้องกระเพื่อมทุกก้าว | detect ตก + ReID feature เพี้ยน |
| **Parallax** | เดินทะลุ 3 มิติ วัตถุใกล้-ไกลเลื่อนไม่เท่ากัน | GMC ชดเชยได้ไม่หมด (มันสมมติว่าฉากเป็นระนาบเดียว) |
| **Scale เปลี่ยนเร็ว** | เดินเข้าหา/ออกจากคน คนโตขึ้น-เล็กลงเร็ว | Kalman filter ทำนายขนาดพลาด → หลุด track |
| **คนเข้า-ออกเฟรมตลอดเวลา** | เดินผ่านคนไปเรื่อย ๆ | ตัวการหลักของการนับเกิน |
| **Rolling shutter** | sensor มือถือ/webcam + การขยับ | ภาพเบี้ยว detect เพี้ยน |

**สิ่งที่ต้องทำต่างจากกล้องนิ่ง:**

1. **FPS ต้องสูงกว่าปกติ (เป้า ≥ 20)** — ไม่ใช่เพื่อความลื่นตา แต่เพราะการจับคู่ track อาศัยว่าตำแหน่งของคนระหว่าง 2 เฟรมติดกันต้องใกล้กันพอ ยิ่งกล้องเดินเร็ว ระยะเลื่อนต่อเฟรมยิ่งมาก **FPS ต่ำ = tracking พังก่อนเป็นอย่างแรก** ไม่ใช่แค่ภาพกระตุก
2. **ล็อกค่าแสง (exposure) ให้ shutter เร็วไว้** — ถ้าปล่อย auto ในที่แสงน้อย กล้องจะลด shutter speed → motion blur หนัก → detect ตกทันที ยอมภาพมืดกว่าดีกว่าภาพเบลอ
3. **`track_buffer` ควรสั้นกว่าเคสกล้องหมุน** — เพราะพฤติกรรม "เดินผ่านแล้วผ่านเลย" คนที่ออกนอกเฟรมส่วนใหญ่ **ไม่กลับมาอีก** การเก็บ track ตายไว้นานมีแต่ทำให้จับคู่ผิดกับคนใหม่
4. **golden clips ต้องถ่ายด้วยการถือเดินจริง** — ถ้าจูนด้วยคลิปจากขาตั้งแล้วเอาไปใช้ถือเดิน ค่าที่จูนมาจะใช้ไม่ได้เลย

> 🔴 **ข้อจำกัดเชิงตรรกะที่แก้ด้วยเทคนิคไม่ได้:** ถ้าเดินวนกลับมาที่เดิม จะเจอคนกลุ่มเดิมซ้ำ และระบบ **จะนับซ้ำแน่นอน** เพราะ track เดิมตายไปนานแล้ว (ReID เก็บ feature ข้ามเวลาเป็นนาที ๆ ไม่ไหว) → นิยามที่ถูกต้องของตัวเลขนี้คือ **"จำนวนคนไม่ซ้ำที่พบระหว่างการเดิน 1 รอบ"** ไม่ใช่ "จำนวนคนทั้งหมดในพื้นที่" ต้องออกแบบเส้นทางเดินไม่ให้วนซ้ำ

---

## 2. Architecture

### 2.1 Pipeline

```
┌─────────────────┐
│  Camera Source  │  ← สลับได้จาก config: webcam(0) / RTSP มือถือ / ไฟล์วิดีโอ
└────────┬────────┘
         ↓ frame (BGR)
┌─────────────────┐
│    Detector     │  ← YOLO11n, filter class=person เท่านั้น
└────────┬────────┘
         ↓ bounding boxes + confidence
┌─────────────────┐
│    Tracker      │  ← BoT-SORT (ReID + Camera Motion Compensation)
└────────┬────────┘
         ↓ track_id ต่อคน
┌─────────────────┐
│    Counter      │  ← pure function: set ของ track_id ที่ confirm แล้ว
└────────┬────────┘
         ↓
┌─────────────────┐
│  Output Layer   │  ← overlay สด + event log (ไม่เก็บ frame)
└─────────────────┘
```

### 2.2 ทำไมต้อง BoT-SORT ไม่ใช่ ByteTrack

| | ByteTrack | BoT-SORT |
|---|---|---|
| Motion matching (Kalman) | ✅ | ✅ |
| **Camera Motion Compensation** | ❌ | ✅ **จำเป็นสำหรับกล้องขยับ** |
| **Appearance / ReID** | ❌ | ✅ ช่วยกันนับซ้ำตอนคนหายไปแล้วกลับมา |
| ความเร็ว | เร็วกว่า | ช้ากว่า (มี ReID inference เพิ่ม) |

**สรุป:** กล้องขยับ → ต้องใช้ BoT-SORT + เปิด `with_reid` ถ้าปิด ReID ระบบจะนับซ้ำหนักมาก

**การเลือก `gmc_method` (ตัวชดเชยการขยับของกล้อง) — สำคัญมากสำหรับการถือเดิน:**

| ค่า | ความเร็ว | ความแม่น | เหมาะกับ |
|---|---|---|---|
| `sparseOptFlow` | เร็วสุด | ปานกลาง | ✅ **ค่าเริ่มต้นที่แนะนำ** — เริ่มจากตัวนี้ |
| `orb` | ปานกลาง | ดีกว่า | ลองถ้า sparseOptFlow ชดเชยไม่พอ |
| `ecc` | ช้า | ดีสุด | ลองเป็นตัวเทียบ แต่มักกิน FPS จนไม่คุ้ม |
| `none` | — | — | ❌ ห้ามใช้ เท่ากับปิดการชดเชยทิ้ง |

> เดินเร็ว/ทางขรุขระ → `sparseOptFlow` อาจหา feature point ที่นิ่งพอไม่ได้ ให้ทดสอบ `orb` เทียบด้วยที่ Phase 3 อย่าเชื่อค่า default อย่างเดียว

### 2.3 Portability Guardrails 🔑

เพื่อให้ port ไป mobile ได้โดยไม่ต้องรื้อ — **บังคับตั้งแต่ Phase 0:**

| กฎ | เหตุผล |
|---|---|
| ใช้ **YOLO11n** ตั้งแต่แรก ห้ามใช้ s/m/l แม้ PC จะไหว | เลือกใหญ่แล้วพอร์ตลงมือถือไม่ได้ ต้อง tune ใหม่หมด |
| ค่า threshold ทุกตัวอยู่ใน `config.json` ห้ามฝังในโค้ด | ค่าที่ tune บน PC ต้องยกไปใช้บนมือถือได้ตรง ๆ |
| `counter.py` เป็น **pure function** ไม่ import OpenCV | ตรรกะการนับต้องแปลงเป็น Kotlin ได้แบบ 1:1 |
| ห้ามใช้ custom layer/op ที่ export TFLite ไม่ได้ | ติดตอน export แล้วแก้ยาก |
| แยก interface ของ camera source ออกจาก main loop | มือถือใช้ CameraX ไม่ใช่ OpenCV VideoCapture |

### 2.4 ความจริงเรื่องการ Port ไป Mobile

| ส่วนประกอบ | PC (Python) | Mobile on-device | ความยาก |
|---|---|---|---|
| Detection | Ultralytics `.pt` | export → TFLite / NCNN | 🟢 ง่าย มีเครื่องมือพร้อม |
| **Tracking** | BoT-SORT พร้อมใช้ | ⚠️ **ไม่มีไลบรารีสำเร็จ** | 🔴 **ยากที่สุดของโปรเจกต์** |
| Counter | pure function | แปลงเป็น Kotlin | 🟢 ง่ายถ้าทำตาม guardrail |
| Camera | OpenCV | CameraX / AVFoundation | 🟡 ปานกลาง |

> 🔴 **ตัวที่ยากคือ tracker ไม่ใช่โมเดล** — บนมือถือมีทางเลือก 2 ทาง: (1) implement ByteTrack เองใน Kotlin แล้วยอมรับว่าแม่นน้อยกว่า เพราะไม่มี ReID (2) implement BoT-SORT + ReID เอง ซึ่งใช้เวลามาก ตัดสินใจตอน Phase 5 หลังเห็นตัวเลขจริงจาก PC แล้ว

---

## 3. Tech Stack

| ชั้น | เทคโนโลยี | หมายเหตุ |
|---|---|---|
| ภาษา | Python 3.10+ | Phase 0–4 |
| Detection | Ultralytics (YOLO11n) | pretrained COCO, ใช้เฉพาะ class 0 = person |
| Tracking | BoT-SORT (มากับ Ultralytics) | เปิด ReID |
| Video I/O | OpenCV | |
| Config | JSON | ค่าทุกตัวอยู่ที่เดียว |
| Log | JSONL (append-only) | ไม่มีภาพ |
| Mobile (Phase 5) | Kotlin + TFLite/NCNN | Android ก่อน |

---

## 4. รายละเอียดแต่ละ Phase

### 🔧 Phase 0 — Foundation

**เป้าหมาย:** ต่อกล้องให้เห็นภาพสด + รู้ขีดจำกัดของเครื่องก่อนใส่โมเดล

**Prerequisite:** ไม่มี (เริ่มได้ทันที)

**งานย่อย:**
- [ ] ตั้ง virtual environment + `pip install ultralytics opencv-python`
- [ ] ตรวจสอบว่า GPU/CUDA ใช้ได้หรือไม่ (`torch.cuda.is_available()`) — ถ้าไม่ได้ ต้องรู้ตั้งแต่ตอนนี้
- [ ] สร้างโครงไฟล์: `config.json`, `camera.py`, `detector.py`, `tracker.py`, `counter.py`, `main.py`
- [ ] `camera.py` — abstraction รับ source จาก config (`0` = webcam / `"rtsp://..."` = มือถือ / `"clip.mp4"` = ไฟล์)
- [ ] วัด FPS ดิบตอนอ่านภาพอย่างเดียว ยังไม่มีโมเดล
- [ ] `.gitignore` + git init + tag `phase-0`

**Deliverable:** โครงโปรเจกต์ที่เปิดกล้องแล้วเห็นภาพสด สลับ source ได้ด้วยการแก้ config อย่างเดียว

**Definition of Done:**
- ✅ เห็นภาพสดจาก webcam
- ✅ เปลี่ยน source เป็นไฟล์วิดีโอได้โดยไม่แก้โค้ด
- ✅ รู้ตัวเลข FPS ดิบ และรู้ว่ามี GPU ให้ใช้หรือไม่

**ความเสี่ยง:** ไดรเวอร์กล้อง/permission บน OS บางตัว, CUDA version ไม่ตรงกับ torch

**เวลาโดยประมาณ:** 0.5–1 วัน

---

### 🎯 Phase 1 — Detection (นับสดในเฟรม)

**เป้าหมาย:** ตรวจจับคนได้ + รู้ FPS จริงตอนรันโมเดล ก่อนลงทุนทำ tracking

**Prerequisite:** Phase 0 ผ่าน

**งานย่อย:**
- [ ] โหลด YOLO11n pretrained → `detector.py`
- [ ] filter เฉพาะ `classes=[0]` (person) — สำคัญ ไม่งั้นจะได้ 80 class
- [ ] วาด bounding box + confidence บนภาพ
- [ ] แสดง occupancy count (จำนวนคนในเฟรม ณ ขณะนั้น)
- [ ] วัด FPS จริงตอนรัน inference + ทดลองปรับ `imgsz` (640 → 480 → 320) ดูผลต่อ FPS
- [ ] ทดลอง `conf` threshold หาจุดที่ false positive น้อยแต่ไม่ตกคน
- [ ] git tag `phase-1`

**Deliverable:** โปรแกรมที่เปิดกล้องแล้วนับคนในเฟรมได้แบบสด

**Definition of Done:**
- ✅ ตรวจจับคนได้ถูกต้องด้วยตาเปล่าในสภาพแสงปกติ
- ✅ นับจำนวนคนในเฟรมได้ (ยังไม่นับสะสม)
- 🚧 **FPS ≥ 20** (เกณฑ์สูงกว่าปกติเพราะกล้องถือเดิน — ดูหัวข้อ 1.5)

> 🚧 **GATE — จุดหยุดสำคัญ**
> ถ้า FPS < 15 **ห้ามไป Phase 2** ให้หยุดแก้ที่ต้นเหตุก่อน ตามลำดับ:
> 1. ลด `imgsz` (640 → 480 → 320)
> 2. เปิด GPU / ใช้ half precision (FP16)
> 3. export โมเดลเป็น ONNX / TensorRT
> 4. ถ้ายังไม่ได้ → ทบทวน hardware
>
> **เหตุผล:** tracking จะกิน FPS เพิ่มอีก 20–40% (ReID ต้อง inference เพิ่ม) ถ้าฐานยังไม่พอ ใส่ tracking ไปก็ใช้ไม่ได้ และจะแยกไม่ออกว่าช้าเพราะอะไร

**ความเสี่ยง:** ครั้งแรกจะดาวน์โหลด weight อัตโนมัติ ต้องมีเน็ต; แสงย้อน/กลางคืนอาจ detect ตก

**เวลาโดยประมาณ:** 1 วัน

---

### 🔗 Phase 2 — Tracking + Unique Count

**เป้าหมาย:** นับสะสมไม่ซ้ำคนได้ — หัวใจของโปรเจกต์

**Prerequisite:** Phase 1 ผ่าน Gate FPS

**งานย่อย:**
- [ ] เปลี่ยนจาก `model.predict()` เป็น `model.track(persist=True, tracker="botsort.yaml")`
- [ ] คัดลอก `botsort.yaml` มาเป็นของตัวเอง → เปิด `with_reid: true` + `gmc_method: sparseOptFlow`
- [ ] `counter.py` — pure function:
  - เก็บ `set` ของ track_id ที่ **confirm แล้ว**
  - track จะถูกนับก็ต่อเมื่อ **อยู่รอดครบ N เฟรม** (`min_track_age_frames`) — กัน track ผี/false positive แวบเดียวมาเพิ่มยอด
- [ ] แสดง 2 ตัวเลขคู่กัน: occupancy (สด) + unique total (สะสม)
- [ ] Event log JSONL: `{timestamp, event: "new_track"|"lost", track_id, unique_total}` — **ไม่มีภาพ**
- [ ] วัด FPS ใหม่หลังใส่ tracking
- [ ] git tag `phase-2`

**Deliverable:** ระบบนับสะสมไม่ซ้ำคนที่ทำงานได้จริง

**Definition of Done:**
- ✅ เดินผ่านกล้อง 5 คน → นับได้ 5 (±1)
- ✅ ยืนนิ่งหน้ากล้อง 30 วินาที → ยอดสะสมไม่เพิ่ม
- ✅ FPS ยังอยู่ในเกณฑ์รับได้

**ความเสี่ยง:** ID switch ตอนคนเดินสวนกัน, track ผีจาก false positive, FPS ตกจาก ReID

**เวลาโดยประมาณ:** 2–3 วัน

---

### 📊 Phase 3 — Golden Set + Tuning

**เป้าหมาย:** จูนให้แม่นถึงเป้า **และ** สร้างชุดทดสอบที่จะใช้ยาวไปถึง Phase 5

**Prerequisite:** Phase 2 ผ่าน

> 🔑 **Phase นี้สำคัญกว่าที่คิด** — golden clips + ตัวเลข expected count ที่เก็บไว้ที่นี่ จะกลายเป็น **regression test** ตอน port ไปมือถือ ทำให้รู้ว่าเวอร์ชันมือถือแม่นตกไปเท่าไหร่ แทนที่จะเดา ถ้าข้าม phase นี้ Phase 5 จะไม่มีอะไรวัดเลย

**งานย่อย:**
- [ ] เก็บ **golden clips 5–10 คลิป** จากสถานที่/มุมกล้องจริง แต่ละคลิป 30–60 วินาที ครอบคลุม:
  - คนเดินผ่านทีละคน (baseline ง่ายสุด)
  - คนเดินสวนกัน / บังกัน (occlusion)
  - คนเดินออกนอกเฟรมแล้วกลับเข้ามา
  - กลุ่มคนหนาแน่น
  - แสงต่างกัน (กลางวัน / ในร่ม / แสงย้อน)
  - **เดินเร็ว vs เดินช้า** (ทดสอบขีดจำกัดของ GMC)
  - **พื้นผิวต่างกัน** (พื้นเรียบ vs ขรุขระ/บันได — ระดับการสั่นต่างกันมาก)

> 🔴 **ทุกคลิปต้องถ่ายด้วยการถือเดินจริง** ห้ามใช้ขาตั้งหรือวางนิ่ง — ค่าที่จูนจากคลิปกล้องนิ่งจะใช้กับงานจริงไม่ได้เลย
- [ ] นับด้วยตาว่าแต่ละคลิปมีคนไม่ซ้ำกี่คน → บันทึกเป็น `golden_set.json`
- [ ] สร้าง `evaluate.py` — รันทุกคลิปแล้วรายงาน error % เทียบ ground truth
- [ ] Tune ทีละพารามิเตอร์ (**ห้ามแก้หลายตัวพร้อมกัน** จะไม่รู้ว่าอะไรมีผล):

| พารามิเตอร์ | ผลกระทบ |
|---|---|
| `track_buffer` | รอกี่เฟรมก่อนถือว่า track หายจริง — สูงไป = คนใหม่ถูกจับเป็นคนเก่า, ต่ำไป = นับซ้ำ · **ถือเดิน → เริ่มจากค่าต่ำ (~30) เพราะคนที่ออกเฟรมมักไม่กลับมา** |
| `gmc_method` | ตัวชดเชยการขยับกล้อง — ทดสอบ `sparseOptFlow` เทียบ `orb` (ดูตารางหัวข้อ 2.2) |
| `match_thresh` | ความเข้มในการจับคู่ track เดิม |
| `new_track_thresh` | conf ขั้นต่ำในการสร้าง track ใหม่ — สูงขึ้น = track ผีน้อยลง |
| `appearance_thresh` / `proximity_thresh` | ความเข้มของ ReID matching |
| `min_track_age_frames` | กัน track ผีมาเพิ่มยอด |

- [ ] บันทึกผลทุกรอบเป็นตาราง (ค่าที่ลอง → error %) เก็บไว้เป็นหลักฐาน
- [ ] git tag `phase-3`

**Deliverable:** `config.json` ที่จูนแล้ว + `golden_set.json` + `evaluate.py` + ตารางผลการจูน

**Definition of Done:**
- ✅ Count error ≤ 15% บน golden set
- ✅ รัน `evaluate.py` แล้วได้ตัวเลขซ้ำเดิมทุกครั้ง (deterministic)
- ✅ มีเอกสารว่าค่าไหนถูกเลือกและทำไม

**ความเสี่ยง:** จูนจนดีเกินไปบน golden set แต่ใช้จริงแล้วแย่ (overfitting) → **กันโดยแบ่งคลิปเป็น tune set กับ holdout set ที่ไม่แตะระหว่างจูน**

**เวลาโดยประมาณ:** 3–5 วัน (ส่วนใหญ่หมดไปกับการเก็บและนับคลิป)

---

### 📦 Phase 4 — Ship PC Version

**เป้าหมาย:** ใช้งานได้จริงในสภาพหน้างาน

**Prerequisite:** Phase 3 ผ่านเกณฑ์

**งานย่อย:**
- [ ] Live overlay ที่อ่านง่าย: unique total ตัวใหญ่ + occupancy + FPS
- [ ] Hotkey: reset counter / pause / quit
- [ ] Graceful shutdown — ปิด camera + เขียน log ปิดท้ายให้ครบ
- [ ] **ตรวจสอบว่าไม่มีการเขียนภาพลงดิสก์ที่ใดเลย** (grep หา `imwrite`, `VideoWriter`) — ยืนยันข้อกำหนด privacy
- [ ] จัดการ error: กล้องหลุด → reconnect อัตโนมัติ ไม่ crash
- [ ] `README.md` — วิธีติดตั้ง รัน และความหมายของทุกค่าใน config
- [ ] git tag `v1.0-pc`

**Deliverable:** เวอร์ชันใช้งานจริงบน PC

**Definition of Done:**
- ✅ รันต่อเนื่อง 1 ชั่วโมงโดยไม่ crash และ memory ไม่บวม
- ✅ ถอดกล้องออกแล้วเสียบใหม่ → ระบบกลับมาทำงานเอง
- ✅ ยืนยันว่าไม่มีไฟล์ภาพถูกสร้างขึ้นเลย

**ความเสี่ยง:** memory leak จากการสะสม track history — ต้องมีขอบเขต (จำกัดขนาดของ set/history)

**เวลาโดยประมาณ:** 2–3 วัน

---

### 📱 Phase 5 — Port ไป Mobile On-device

**เป้าหมาย:** รันบนมือถือโดยไม่ต้องพึ่งคอม

**Prerequisite:** Phase 4 เสร็จ + มี golden set ครบ

**งานย่อย:**
- [ ] Export โมเดล: `yolo export model=yolo11n.pt format=tflite int8=True`
- [ ] **วัดก่อนว่า quantize แล้วความแม่นตกเท่าไหร่** — รัน golden set ด้วยโมเดลที่ export แล้วบน PC เทียบกับ `.pt` เดิม (ทำบน PC ก่อน จะได้แยกออกว่าที่แม่นตกเกิดจาก quantization หรือจาก tracker ที่เปลี่ยน)
- [ ] ตัดสินใจ tracker บนมือถือ:
  - **ทางเลือก A:** ByteTrack ใน Kotlin — งานน้อยกว่า แต่ไม่มี ReID → นับซ้ำมากขึ้นเมื่อกล้องขยับ
  - **ทางเลือก B:** BoT-SORT + ReID เต็มรูปแบบ — แม่นกว่า แต่ต้อง implement เอง + inference เพิ่มอีกตัว
  - 🛑 **ตัดสินใจหลังเห็นตัวเลขจากข้อก่อนหน้า ไม่ใช่เดาตอนนี้**
- [ ] Android app: CameraX → TFLite (เปิด GPU/NNAPI delegate) → tracker → counter
- [ ] แปลง `counter.py` เป็น Kotlin แบบ 1:1
- [ ] **Validate ด้วย golden set เดิม** โดยป้อนเป็น**ไฟล์วิดีโอ** ไม่ใช่กล้องสด → เทียบกันได้ตรง ๆ กับผลจาก PC
- [ ] git tag `v1.0-mobile`

**Deliverable:** แอป Android ที่นับคนได้แบบ on-device

**Definition of Done:**
- ✅ รันได้ ≥ 15 FPS บนเครื่องเป้าหมาย
- ✅ มีตารางเทียบ error % ระหว่างเวอร์ชัน PC กับ mobile ชัดเจน
- ✅ ตัดสินใจอย่างมีข้อมูลว่าความแม่นที่ลดลงยอมรับได้หรือไม่

**ความเสี่ยง:** 🔴 tracker เป็นงานที่ประเมินต่ำเกินไปได้ง่ายที่สุด — เผื่อเวลาไว้มาก; ความร้อนของเครื่องทำให้ throttle FPS ตกหลังรันไปสักพัก

**เวลาโดยประมาณ:** 1–2 สัปดาห์

---

## 5. Pre-mortem — สมมติว่าโปรเจกต์ล้มเหลว เพราะอะไร

| # | ความเสี่ยง | โอกาส | ผลกระทบ | วิธีป้องกัน |
|---|---|---|---|---|
| R1 | FPS ไม่พอเพราะกล้องขยับต้อง detect ทุกเฟรม | สูง | 🔴 ใช้ไม่ได้ | Gate ที่ Phase 1, YOLO11n, ลด imgsz |
| R2 | ID switch ตอนคนเดินสวนกัน → นับเกิน | สูง | 🟡 error สูง | เปิด ReID, tune ที่ Phase 3 |
| R3 | คนออกนอกเฟรมแล้วกลับเข้ามา → นับซ้ำ | สูง | 🟡 error สูง | tune `track_buffer`, ยอมรับ error floor |
| R4 | ไม่มีภาพเก็บไว้ debug ตอนเจอปัญหาหน้างาน | กลาง | 🟡 หา root cause ไม่ได้ | เก็บ golden clips ช่วง dev เท่านั้น |
| R5 | Overfit บน golden set | กลาง | 🟡 หลอกตัวเอง | แยก holdout set ที่ไม่แตะตอนจูน |
| R6 | Tracker บนมือถือกินเวลาบานปลาย | สูง | 🔴 Phase 5 ยืด | ยอมถอยไป ByteTrack, ประเมินหลังมีตัวเลข |
| R7 | Memory leak จากการสะสม track history | กลาง | 🟡 crash ระยะยาว | จำกัดขนาด history, ทดสอบรัน 1 ชม. |
| R8 | ประเด็นความเป็นส่วนตัวหน้างาน | กลาง | 🔴 ใช้ไม่ได้เลย | ดูหัวข้อ 7 |
| R9 | **เดินวนกลับที่เดิม → เจอคนกลุ่มเดิมซ้ำ** | สูง | 🔴 ตัวเลขผิดโดยพื้นฐาน | ออกแบบเส้นทางเดินไม่ให้วนซ้ำ + นิยามตัวเลขให้ถูก (หัวข้อ 1.5) |
| R10 | **Motion blur จากการก้าวเดิน** | สูง | 🟡 detect ตก | ล็อก exposure ให้ shutter เร็ว, เลี่ยงที่แสงน้อย |
| R11 | **Parallax ทำให้ GMC ชดเชยไม่หมด** | สูง | 🟡 track หลุด | ทดสอบ gmc_method หลายตัวที่ Phase 3 |

---

## 6. Rollback Strategy

**หลักการ:** ทุก phase = git tag แยก ถอยกลับไปเวอร์ชันที่ใช้งานได้เสมอ

| สถานการณ์ | การถอย |
|---|---|
| Phase 2 นับไม่แม่นพอ แก้ไม่ตก | ถอยไปใช้ **occupancy count จาก Phase 1** ซึ่งทำงานได้อยู่แล้ว — ยังมีของส่งมอบ |
| Phase 3 จูนแล้วแย่ลง | `git checkout` config เวอร์ชันก่อน (เก็บทุกรอบเป็นตาราง) |
| Phase 5 มือถือแม่นไม่พอ | กลับไปใช้ PC version + มือถือทำหน้าที่เป็นกล้องส่งภาพผ่าน RTSP แทน |

**Fallback ที่ยอมรับได้:** ถ้า unique count แม่นไม่ถึงเป้าจริง ๆ occupancy count ยังเป็นของที่ใช้งานได้และส่งมอบได้ ไม่ใช่ศูนย์

---

## 7. Security & Privacy

| ประเด็น | แนวทาง |
|---|---|
| ไม่เก็บภาพใน production | ✅ ข้อได้เปรียบใหญ่ด้าน PDPA — ต้อง verify ด้วย grep ที่ Phase 4 |
| ReID embedding | เป็นข้อมูล appearance ของบุคคล → **เก็บใน RAM เท่านั้น ห้าม persist ลงดิสก์** และเคลียร์เมื่อ track หมดอายุ |
| Golden clips ช่วง dev | มีภาพคนจริง → เก็บในเครื่อง ไม่ขึ้น cloud/git, ใส่ใน `.gitignore`, ลบเมื่อจบ Phase 5 |
| ติดตั้งพื้นที่สาธารณะ | ควรมีป้ายแจ้งว่ามีระบบนับจำนวนคน (ไม่ระบุตัวตน) |
| Log | เก็บเฉพาะตัวเลขและ timestamp — ห้ามมี path ของภาพ |

---

## 8. ตัวอย่าง config.json

```json
{
  "camera": {
    "source": 0,
    "width": 1280,
    "height": 720
  },
  "detector": {
    "model": "yolo11n.pt",
    "imgsz": 640,
    "conf": 0.35,
    "iou": 0.5,
    "classes": [0],
    "device": "cuda:0",
    "half": true
  },
  "tracker": {
    "config_file": "botsort_custom.yaml",
    "with_reid": true,
    "gmc_method": "sparseOptFlow",
    "track_buffer": 60,
    "match_thresh": 0.8,
    "new_track_thresh": 0.6
  },
  "counter": {
    "min_track_age_frames": 5,
    "max_history": 10000
  },
  "output": {
    "show_window": true,
    "log_path": "logs/events.jsonl",
    "save_frames": false
  }
}
```

> ⚠️ ชื่อพารามิเตอร์ของ tracker ให้ตรวจสอบกับ `botsort.yaml` ในเวอร์ชัน Ultralytics ที่ติดตั้งจริง อาจต่างกันเล็กน้อยตามเวอร์ชัน

---

## 9. Decision Log

| # | การตัดสินใจ | เหตุผล | สถานะ |
|---|---|---|---|
| D1 | ใช้ deep learning ไม่ใช่ background subtraction | กล้องขยับ ทำให้ background subtraction ใช้ไม่ได้เลย | ✅ ตัดสินแล้ว |
| D2 | นับสะสมไม่ซ้ำคน ไม่ใช่แค่ occupancy | ความต้องการหลักของงาน | ✅ ตัดสินแล้ว |
| D3 | ไม่บันทึกภาพใน production | ความเป็นส่วนตัว + ลดความซับซ้อน | ✅ ตัดสินแล้ว |
| D4 | PC + webcam ก่อน แล้วค่อยไป mobile on-device | ลด risk, มี reference implementation ไว้เทียบ | ✅ ตัดสินแล้ว |
| D5 | YOLO11n (ไม่ใช่รุ่นใหญ่กว่า) | ต้อง port ลงมือถือได้ | ✅ ตัดสินแล้ว |
| D6 | Tracker บนมือถือ: ByteTrack หรือ BoT-SORT | ต้องมีตัวเลขจริงก่อนตัดสิน | ⏳ รอ Phase 5 |
| D7 | Fine-tune โมเดลด้วยข้อมูลตัวเองหรือไม่ | ขึ้นกับผล pretrained ที่ Phase 3 | ⏳ รอ Phase 3 |
| D8 | **กล้องถือเดิน** | ความต้องการหน้างาน | ✅ ตัดสินแล้ว — ปรับเกณฑ์ FPS ขึ้นเป็น 20 + เพิ่มหัวข้อ 1.5 |

---

## 10. Open Questions

1. สถานที่ใช้งานจริงเป็นแบบไหน (ในร่ม/กลางแจ้ง, ความหนาแน่นของคน)
2. **เส้นทางเดินวนกลับมาที่เดิมหรือไม่** — ถ้าวน ตัวเลขจะซ้ำโดยแก้ไม่ได้ (R9) ต้องออกแบบเส้นทางใหม่
3. ยอดสะสมต้อง reset เมื่อไหร่ — ทุกครั้งที่เริ่มเดินรอบใหม่ / manual / ไม่ reset
4. เครื่องที่ใช้ตอนถือเดินคืออะไร (โน้ตบุ๊กสะพาย + webcam / mini PC / มือถือส่ง RTSP) — มีผลต่อ GPU ที่ใช้ได้จริง
5. มีขาจับกล้องหรือ gimbal ไหม — gimbal ช่วยลด motion blur และ parallax ได้มาก คุ้มค่าลงทุนถ้าความแม่นไม่ถึงเป้า

---

## Revision History

| เวอร์ชัน | วันที่ | การเปลี่ยนแปลง |
|---|---|---|
| 1.0 | 2026-08-21 | ฉบับแรก — ครอบคลุม Phase 0–5 |
| 1.1 | 2026-08-21 | ระบุว่ากล้องถือเดิน — เพิ่มหัวข้อ 1.5, ตาราง gmc_method, ปรับเกณฑ์ FPS เป็น 20, เพิ่มความเสี่ยง R9–R11 |
